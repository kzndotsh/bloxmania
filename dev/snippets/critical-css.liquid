{%- comment -%}
  Critical CSS for above-the-fold content
  This snippet contains only the CSS needed for immediate page rendering
  to improve First Contentful Paint (FCP) and Largest Contentful Paint (LCP)
{%- endcomment -%}

/* Critical CSS - Above the fold styles only */

/* CSS Custom Properties - Match existing theme */ :root { --color-background: 29 30 38; --color-surface: 37 39 48;
--color-foreground: 255 255 255; --color-primary: 255 216 0; --color-secondary: 71 145 240; --color-accent: 89 186 185;
--color-border: 51 52 65; --page-width: 1280px; --container-padding: 1.5rem; --font-body-scale: 1; --font-heading-scale:
1.2; --border-radius: 0.5rem; --shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
--duration-default: 300ms; --easing-smooth: cubic-bezier(0.4, 0, 0.2, 1); }

/* Base styles */ *, *::before, *::after { box-sizing: border-box; }

html { font-size: calc(var(--font-body-scale) * 62.5%); height: 100%; scroll-behavior: smooth; }

body { margin: 0; font-family: 'Inter', system-ui, -apple-system, sans-serif; font-size: 1.6rem; line-height: 1.6;
color: rgb(var(--color-foreground)); background-color: rgb(var(--color-background)); -webkit-font-smoothing:
antialiased; -moz-osx-font-smoothing: grayscale; }

/* Layout */ .page-width { max-width: var(--page-width); margin: 0 auto; padding: 0 var(--container-padding); }

.container { width: 100%; max-width: var(--page-width); margin: 0 auto; padding: 0 var(--container-padding); }

/* Typography - Use existing scale */ h1, h2, h3, h4, h5, h6 { margin: 0 0 1rem 0; font-family: 'Inter', system-ui,
-apple-system, sans-serif; font-weight: 700; line-height: 1.2; color: rgb(var(--color-foreground)); }

h1 { font-size: calc(3.2rem * var(--font-heading-scale)); } h2 { font-size: calc(2.8rem * var(--font-heading-scale)); }
h3 { font-size: calc(2.4rem * var(--font-heading-scale)); } h4 { font-size: calc(2rem * var(--font-heading-scale)); } h5
{ font-size: calc(1.8rem * var(--font-heading-scale)); } h6 { font-size: calc(1.6rem * var(--font-heading-scale)); }

p { margin: 0 0 1rem 0; line-height: 1.6; font-size: calc(1.6rem * var(--font-body-scale)); }

a { color: rgb(var(--color-primary)); text-decoration: none; transition: color var(--duration-default)
var(--easing-smooth); }

a:hover { color: rgb(var(--color-secondary)); }

/* Buttons */ .btn { display: inline-flex; align-items: center; justify-content: center; padding: 1rem 2rem; font-size:
calc(1.6rem * var(--font-body-scale)); font-weight: 600; text-decoration: none; border: none; border-radius:
var(--border-radius); cursor: pointer; transition: all var(--duration-default) var(--easing-smooth); background-color:
rgb(var(--color-primary)); color: rgb(0, 0, 0); }

.btn:hover { transform: translateY(-2px); box-shadow: var(--shadow); }

.btn-primary { background-color: rgb(var(--color-primary)); color: rgb(0, 0, 0); }

.btn-secondary { background-color: transparent; color: rgb(var(--color-foreground)); border: 2px solid
rgb(var(--color-primary)); }

/* Header */ header { position: sticky; top: 0; z-index: 1000; background-color: rgba(var(--color-background), 0.95);
backdrop-filter: blur(12px); border-bottom: 1px solid rgba(var(--color-border), 0.1); transition: all
var(--duration-default) var(--easing-smooth); }

.header-content { display: flex; align-items: center; justify-content: space-between; padding: 1rem 0; }

.logo { font-size: calc(2.4rem * var(--font-heading-scale)); font-weight: 700; color: rgb(var(--color-primary));
text-decoration: none; }

/* Navigation */ nav { display: flex; align-items: center; gap: 2rem; }

.nav-link { color: rgb(var(--color-foreground)); text-decoration: none; font-weight: 500; font-size: calc(1.6rem *
var(--font-body-scale)); transition: color var(--duration-default) var(--easing-smooth); }

.nav-link:hover { color: rgb(var(--color-primary)); }

/* Hero Section */ .hero-section { padding: 4rem 0; text-align: center; background: linear-gradient(135deg,
rgb(var(--color-background)) 0%, rgb(var(--color-surface)) 100%); }

.hero-title { font-size: calc(4.8rem * var(--font-heading-scale)); font-weight: 800; margin-bottom: 2rem; background:
linear-gradient(135deg, rgb(var(--color-primary)) 0%, rgb(var(--color-secondary)) 100%); -webkit-background-clip: text;
-webkit-text-fill-color: transparent; background-clip: text; }

.hero-description { font-size: calc(2rem * var(--font-body-scale)); color: rgba(var(--color-foreground), 0.8);
margin-bottom: 3rem; max-width: 60rem; margin-left: auto; margin-right: auto; }

/* Cards */ .card { background-color: rgb(var(--color-surface)); border-radius: var(--border-radius); padding: 2rem;
box-shadow: var(--shadow); transition: all var(--duration-default) var(--easing-smooth); }

.card:hover { transform: translateY(-4px); box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2); }

/* Grid Layout */ .grid { display: grid; gap: 2rem; }

.grid-cols-1 { grid-template-columns: repeat(1, 1fr); } .grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
.grid-cols-3 { grid-template-columns: repeat(3, 1fr); } .grid-cols-4 { grid-template-columns: repeat(4, 1fr); }

/* Responsive */ @media (max-width: 768px) { .hero-title { font-size: calc(3.2rem * var(--font-heading-scale)); }
.hero-description { font-size: calc(1.8rem * var(--font-body-scale)); } .grid-cols-2, .grid-cols-3, .grid-cols-4 {
grid-template-columns: repeat(1, 1fr); } }

@media (min-width: 769px) and (max-width: 1024px) { .grid-cols-3, .grid-cols-4 { grid-template-columns: repeat(2, 1fr);
} }

/* Utility Classes */ .text-center { text-align: center; } .text-left { text-align: left; } .text-right { text-align:
right; }

.mb-1 { margin-bottom: 0.5rem; } .mb-2 { margin-bottom: 1rem; } .mb-3 { margin-bottom: 1.5rem; } .mb-4 { margin-bottom:
2rem; }

.mt-1 { margin-top: 0.5rem; } .mt-2 { margin-top: 1rem; } .mt-3 { margin-top: 1.5rem; } .mt-4 { margin-top: 2rem; }

.p-1 { padding: 0.5rem; } .p-2 { padding: 1rem; } .p-3 { padding: 1.5rem; } .p-4 { padding: 2rem; }

/* Loading states */ .loading { opacity: 0.6; pointer-events: none; }

/* Accessibility */ .visually-hidden { position: absolute !important; width: 1px !important; height: 1px !important;
padding: 0 !important; margin: -1px !important; overflow: hidden !important; clip: rect(0, 0, 0, 0) !important;
white-space: nowrap !important; border: 0 !important; }

/* Focus styles */ *:focus { outline: 2px solid rgb(var(--color-primary)); outline-offset: 2px; }

/* Skip link */ .skip-to-content-link { position: absolute; top: -40px; left: 6px; background:
rgb(var(--color-primary)); color: rgb(0, 0, 0); padding: 8px; text-decoration: none; border-radius:
var(--border-radius); z-index: 10000; font-size: calc(1.4rem * var(--font-body-scale)); }

.skip-to-content-link:focus { top: 6px; }
