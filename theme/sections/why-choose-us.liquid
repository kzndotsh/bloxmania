{%- style -%}
  .why-choose-us {
    padding: 5rem 2rem;
    background: rgba(24, 25, 38, 0.9);
    text-align: center;
  }

  .why-choose-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 3rem;
  }

  .why-choose-title span {
    color: #ffd800;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .feature-card {
    padding: 2rem;
    border-radius: 16px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    text-align: left;
  }

  .feature-card.reliability {
    background: linear-gradient(135deg, #ff6b35, #f7931e);
  }

  .feature-card.security {
    background: linear-gradient(135deg, #e74c3c, #c0392b);
  }

  .feature-card.support {
    background: linear-gradient(135deg, #2ecc71, #27ae60);
  }

  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  }

  .feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transition: left 0.5s;
  }

  .feature-card:hover::before {
    left: 100%;
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    position: relative;
    z-index: 1;
  }

  .feature-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 1rem;
    position: relative;
    z-index: 1;
  }

  .feature-description {
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
    position: relative;
    z-index: 1;
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
    
    .feature-card {
      text-align: center;
    }
  }
{%- endstyle -%}

<section class="why-choose-us">
  <h2 class="why-choose-title">
    Why Choose <span>BloxMania</span>?
  </h2>

  <div class="features-grid">
    <div class="feature-card reliability">
      <div class="feature-icon">⚡</div>
      <h3 class="feature-title">Fast & Reliable</h3>
      <p class="feature-description">
        Our automated system ensures your items are delivered immediately after purchase. No waiting - just instant gratification for all your gaming needs.
      </p>
    </div>

    <div class="feature-card security">
      <div class="feature-icon">🔒</div>
      <h3 class="feature-title">Secure Transactions</h3>
      <p class="feature-description">
        We use trusted payment systems to keep your data safe and secure. Every transaction is protected with industry-standard encryption.
      </p>
    </div>

    <div class="feature-card support">
      <div class="feature-icon">💬</div>
      <h3 class="feature-title">24/7 Support</h3>
      <p class="feature-description">
        Our dedicated support team is always here to help you. If you ever have an issue, we'll resolve it quickly and make sure you're satisfied.
      </p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Why Choose Us",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Why Choose BloxMania?"
    }
  ],
  "presets": [
    {
      "name": "Why Choose Us"
    }
  ]
}
{% endschema %} 