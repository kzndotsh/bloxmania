{%- style -%}
  .hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), url('{{ 'hero.png' | asset_url }}');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    position: relative;
    overflow: hidden;
    padding-top: 8rem;
    padding-bottom: 4rem;
  }

  .hero-content {
    text-align: center;
    max-width: 90rem;
    padding: 3rem 2rem;
    position: relative;
    z-index: 2;
    margin: 0 auto;
  }

  .hero-rating {
    display: flex;
    justify-content: center;
    margin-bottom: 2.5rem;
  }

  .hero-rating img {
    height: 3rem;
    width: auto;
    transition: all 0.3s ease;
    transform: scale(1.1);
    filter: drop-shadow(0 0 4px rgba(255, 216, 0, 0.2));
    cursor: pointer;
  }

  .hero-rating img:hover {
    transform: scale(1.25);
  }

  .hero-title {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    font-weight: 900;
    margin-bottom: 2rem;
    line-height: 1.2;
    color: #ffffff;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  .hero-title-gradient {
    color: #ffd800;
  }

  @keyframes gradient-shift {
    0%,
    100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .hero-subtitle {
    font-size: clamp(1.25rem, 4vw, 1.5rem);
    font-weight: 500;
    color: #ffffff;
    margin-bottom: 3rem;
    max-width: 60rem;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.7;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.4);
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 2.5rem;
    background: #ffd800;
    color: #000;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
    border-radius: 50px;
    box-shadow: 0 3px 15px rgba(255, 216, 0, 0.2);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    text-shadow: 0 1px 1px rgba(0, 0, 0, 0.2);
    margin-bottom: 3.5rem;
  }

  .hero-cta:hover {
    background: #e6c200;
    transform: translateY(-2px);
    box-shadow: 0 5px 20px rgba(255, 216, 0, 0.3);
    color: #000;
  }

  .hero-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }

  .hero-cta:hover::before {
    left: 100%;
  }

  /* Trusted by Creators Section */
  .creators-section {
    margin-top: 2rem;
  }

  .creators-title {
    font-size: clamp(1.5rem, 4vw, 2rem);
    font-weight: 700;
    margin-bottom: 3rem;
    color: #ffffff;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  .creators-title .highlight {
    color: #ffd800;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  .creators-reel {
    position: relative;
    overflow: hidden;
    margin: 0 auto;
    max-width: 1000px;
    mask: linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%);
    -webkit-mask: linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%);
  }

  .creators-scroll {
    display: flex;
    animation: scroll 30s linear infinite;
    gap: 4rem;
  }

  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  .creator-item {
    flex-shrink: 0;
    text-align: center;
    min-width: 8rem;
  }

  .creator-avatar {
    width: 7rem;
    height: 7rem;
    border-radius: 1rem;
    margin: 0 auto 0.5rem;
    transition: all 0.3s ease;
    cursor: pointer;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
  }

  .creator-avatar:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
  }

  .creator-name {
    font-size: 1rem;
    font-weight: 600;
    color: #ffffff;
    margin-bottom: 0.25rem;
    white-space: nowrap;
    text-shadow: 0 1px 1px rgba(0, 0, 0, 0.2);
  }

  .creator-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.25rem;
    font-size: 0.875rem;
    color: #ffffff;
    text-shadow: 0 1px 1px rgba(0, 0, 0, 0.15);
  }

  .youtube-icon {
    width: 0.75rem;
    height: 0.75rem;
    fill: currentColor;
    filter: drop-shadow(0 1px 1px rgba(0, 0, 0, 0.2));
  }

  /* Small Mobile Devices */
  @media (max-width: 480px) {
    .hero-section {
      padding-top: 4.5rem;
      padding-bottom: 2.5rem;
      min-height: 85vh;
    }

    .hero-content {
      padding: 1.5rem 0.75rem;
      max-width: 95%;
    }

    .hero-rating {
      margin-bottom: 1.5rem;
    }

    .hero-rating img {
      height: 2rem;
    }

    .hero-title {
      font-size: clamp(1.75rem, 12vw, 2.5rem);
      margin-bottom: 1.25rem;
      line-height: 1.05;
    }

    .hero-subtitle {
      font-size: clamp(0.9rem, 4.5vw, 1rem);
      margin-bottom: 2rem;
      line-height: 1.5;
    }

    .hero-cta {
      padding: 0.875rem 1.75rem;
      font-size: 0.95rem;
      margin-bottom: 2.5rem;
      min-width: 180px;
    }

    .creators-section {
      margin-top: 1rem;
    }

    .creators-title {
      font-size: clamp(1.125rem, 5vw, 1.25rem);
      margin-bottom: 1.5rem;
    }

    .creators-scroll {
      gap: 2rem;
    }

    .creator-item {
      min-width: 4.5rem;
    }

    .creator-avatar {
      width: 4rem;
      height: 4rem;
    }

    .creator-name {
      font-size: 0.8rem;
    }

    .creator-stats {
      font-size: 0.7rem;
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .hero-section {
      padding-top: 5rem;
      padding-bottom: 3rem;
      background-attachment: scroll;
      min-height: 90vh;
    }

    .hero-content {
      padding: 2rem 1rem;
    }

    .hero-rating {
      margin-bottom: 2rem;
    }

    .hero-rating img {
      height: 2.25rem;
    }

    .hero-title {
      font-size: clamp(2rem, 10vw, 3rem);
      margin-bottom: 1.5rem;
      line-height: 1.1;
    }

    .hero-subtitle {
      font-size: clamp(1rem, 5vw, 1.125rem);
      margin-bottom: 2.5rem;
      max-width: 100%;
      line-height: 1.6;
    }

    .hero-cta {
      padding: 1rem 2rem;
      font-size: 1rem;
      margin-bottom: 3rem;
      width: auto;
      min-width: 200px;
    }

    .creators-section {
      margin-top: 1.5rem;
    }

    .creators-title {
      font-size: clamp(1.25rem, 6vw, 1.5rem);
      margin-bottom: 2rem;
    }

    .creators-reel {
      max-width: 100%;
      margin: 0 -1rem;
    }

    .creators-scroll {
      gap: 2.5rem;
    }

    .creator-item {
      min-width: 5rem;
    }

    .creator-avatar {
      width: 4.5rem;
      height: 4.5rem;
    }

    .creator-name {
      font-size: 0.875rem;
    }

    .creator-stats {
      font-size: 0.75rem;
    }
  }

  @media (min-width: 1024px) {
    .hero-title {
      font-size: clamp(3rem, 8vw, 5rem);
    }

    .creators-title {
      font-size: clamp(2rem, 4vw, 2.5rem);
    }

    .creator-avatar {
      width: 8rem;
      height: 8rem;
    }

    .creator-item {
      min-width: 9rem;
    }
  }

  /* Pause animation on hover */
  .creators-reel:hover .creators-scroll {
    animation-play-state: paused;
  }
{%- endstyle -%}

<section class="hero-section">
  <div class="hero-content">
    <!-- Rating Badge -->
    <div class="hero-rating">
      <img
        src="{{ 'ratingz.png' | asset_url }}"
        alt="5-Star Rating"
        width="120"
        height="40"
        loading="lazy"
      >
    </div>

    <!-- Main Title -->
    <h1 class="hero-title">
      Instantly Buy Your Favorite Items
      <br>
      <span class="hero-title-gradient">Fast, Safe, and Easy!</span>
    </h1>

    <!-- Subtitle -->
    <p class="hero-subtitle">
      {% if section.settings.subtitle != blank %}
        {{ section.settings.subtitle }}
      {% else %}
        BloxMania is the fastest, cheapest, and safest shop for digital goods, with instant delivery. Our
        <strong>automated system</strong> ensures you receive your items securely and swiftly!
      {% endif %}
    </p>

    <!-- Call to Action -->
    <a href="{{ section.settings.cta_link | default: '/collections/all' }}" class="hero-cta">
      {{ section.settings.cta_text | default: 'SHOP NOW' }}
    </a>

    <!-- Trusted by Top Creators Section -->
    <div class="creators-section">
      <h3 class="creators-title">Trusted by <span class="highlight">Top</span> Creators</h3>

      <div class="creators-reel">
        <div class="creators-scroll">
          <!-- First set of creators -->
          {% assign creators = 'Ali-A,19.6M,https://www.youtube.com/@AliA|CaseOh,8M,https://www.youtube.com/@caseoh_|DanTDM,29M,https://www.youtube.com/@DanTDM|Flamingo,13M,https://www.youtube.com/@Flamingo|KreekCraft,12M,https://www.youtube.com/@KreekCraft|LanasLife,7.5M,https://www.youtube.com/@Lanaslifeee|SypherPK,10.6M,https://www.youtube.com/@SypherPK|TypicalGamer,15M,https://www.youtube.com/@TypicalGamer'
            | split: '|'
          %}

          {% for creator_data_string in creators %}
            {% assign creator_data = creator_data_string | split: ',' %}
            <div class="creator-item">
              <a href="{{ creator_data[2] }}" target="_blank" rel="noopener noreferrer">
                <img
                  src="{{ creator_data[0] | append: '.png' | asset_url }}"
                  alt="{{ creator_data[0] }}"
                  class="creator-avatar"
                  width="112"
                  height="112"
                  loading="lazy"
                >
              </a>
              <p class="creator-name">{{ creator_data[0] }}</p>
              <div class="creator-stats">
                <svg class="youtube-icon" viewBox="0 0 24 24">
                  <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
                </svg>
                <span>{{ creator_data[1] }}</span>
              </div>
            </div>
          {% endfor %}

          <!-- Duplicate set for seamless loop -->
          {% for creator_data_string in creators %}
            {% assign creator_data = creator_data_string | split: ',' %}
            <div class="creator-item">
              <a href="{{ creator_data[2] }}" target="_blank" rel="noopener noreferrer">
                <img
                  src="{{ creator_data[0] | append: '.png' | asset_url }}"
                  alt="{{ creator_data[0] }}"
                  class="creator-avatar"
                  width="112"
                  height="112"
                  loading="lazy"
                >
              </a>
              <p class="creator-name">{{ creator_data[0] }}</p>
              <div class="creator-stats">
                <svg class="youtube-icon" viewBox="0 0 24 24">
                  <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
                </svg>
                <span>{{ creator_data[1] }}</span>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "text",
      "id": "title_part1",
      "label": "Hero Title Part 1",
      "default": "Instantly Buy Your Favorite Items"
    },
    {
      "type": "text",
      "id": "title_part2",
      "label": "Hero Title Part 2 (Yellow)",
      "default": "Fast, Safe, and Easy!"
    },
    {
      "type": "richtext",
      "id": "subtitle",
      "label": "Hero Subtitle",
      "default": "<p>BloxMania is the fastest, cheapest, and safest shop for digital goods, with instant delivery. Our <strong>automated system</strong> ensures you receive your items securely and swiftly!</p>"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Button Text",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Button Link"
    }
  ],
  "presets": [
    {
      "name": "Hero Section"
    }
  ]
}
{% endschema %}
